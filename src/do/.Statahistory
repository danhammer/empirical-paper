use final, clear
desc
list in 1/100
desc
list pd cntry prop post in 1/100
help hpfilter
findit hpfilter
ls
desc
outsheet pd cntry prop post using /tmp/data
outsheet pd cntry prop post using /tmp/data/tester.csv, comma
outsheet using pd cntry prop post using /tmp/data/tester.csv, comma
outsheet pd cntry prop post using /tmp/tester.csv, comma
reg prop pd if cntry == 0 & post == 1
reg prop pd if cntry == 0 & post == 0
reg prop pd if cntry == 1 & post == 0
reg prop pd if cntry == 1 & post == 1
use $base_dir/src/do/final, clear
desc
help outreg2
desc
rename i_pd_post pd:post
rename i_pd_post pd-post
rename i_pd_post pd.post
use $base_dir/src/do/total_clusters
use $base_dir/src/do/total_clusters, clear
